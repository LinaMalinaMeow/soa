spring:
  datasource:
    url: 'jdbc:postgresql://localhost:5432/studs'
    username: s311695
    password: ciyZhZk2l1jTO9FP
    driver-class-name: org.postgresql.Driver
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQL9Dialect
    hibernate:
      ddl-auto: update
    show-sql: true